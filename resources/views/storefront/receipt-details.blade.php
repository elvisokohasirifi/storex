<p class="eyebrow">SALES RECEIPT</p><h1>{{ $order->shop->name }}</h1><p>Reference: {{ $order->reference }}</p><p>Status: <strong>{{ str_replace('_', ' ', ucfirst($order->status)) }}</strong></p>
@if($order->status === 'paid_review')<p class="notice">Your payment was received and needs the shop's attention before fulfilment. Please contact {{ $order->shop->contacts }}.</p>@endif
<table class="receipt-table table"><thead><tr><th>Item</th><th>Quantity</th><th>Amount</th></tr></thead><tbody>@foreach($order->items as $item)<tr><td>{{ $item->name }}</td><td>{{ $item->quantity }}</td><td>{{ $order->currency }} {{ number_format($item->quantity * $item->unit_price / 100, 2) }}</td></tr>@endforeach</tbody></table><h2>Total: {{ $order->currency }} {{ number_format($order->total / 100, 2) }}</h2><p>{{ $order->created_at->format('d M Y, H:i') }} · {{ ucfirst($order->channel) }}</p>
