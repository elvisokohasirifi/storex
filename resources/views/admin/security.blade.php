@extends(backpack_view('blank'))
@section('header')<section class="container-fluid"><h1>Login & security</h1></section>@endsection
@section('content')@include('admin.messages')<div class="card"><div class="card-body"><h3>Phone & PIN login</h3><p>Use an international phone number, including its country code. Leave both phone and PIN blank to disable this login method. Google-only users can use password recovery to set a password first.</p><form method="post" action="{{ route('account.security.update') }}">@csrf<label class="form-label w-100">Current password<input class="form-control" type="password" name="password" required autocomplete="current-password"></label><label class="form-label w-100">Phone<input class="form-control" name="phone" type="tel" value="{{ old('phone', backpack_user()->phone) }}" placeholder="+233241234567"></label><label class="form-label w-100">New 6-digit PIN<input class="form-control" name="pin" type="password" pattern="[0-9]{6}" maxlength="6" inputmode="numeric" autocomplete="new-password"></label><label class="form-label w-100">Confirm PIN<input class="form-control" name="pin_confirmation" type="password" pattern="[0-9]{6}" maxlength="6" inputmode="numeric" autocomplete="new-password"></label><button class="btn btn-primary">Save phone login</button></form></div></div><div class="card"><div class="card-body"><h3>Google sign-in</h3><p>{{ backpack_user()->google_id ? 'Google is connected to this account.' : 'Connect the Google account that uses the same email as your Storex account.' }}</p><a class="btn btn-outline-primary" href="{{ route('google.redirect') }}">Connect Google</a></div></div>@endsection
